# 变更日志

本文件记录 dsh-workbench 每个版本做了什么。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.2.0] — 2026-09-19

### 目录不用手打了：迭代页与文档页各加一个「浏览…」

- 点「浏览…」开的是**宿主的目录选择**，两条路自动挑：
  1. 宿主挂的是**操作系统原生选择器**（本地 127.0.0.1 部署）→ 弹系统的选文件夹对话框；
  2. 宿主挂的是 **browse 后端**（远程 / SSH 启动 / 其它部署——**本机实测就是这种**）→
     原生 `pick()` 会被宿主明确拒绝（原文 `directoryPicker.pick needs the native capability; the composed picker serves "browse"`），
     这时自动改用**插件自绘的应用内目录浏览器**：面包屑 + 子目录列表（隐藏项淡化）+ 手输路径 + 上一层 / 主目录。
     它走同一个后端的 `listDirectory`，和 DSH 官方「添加工作区」在 browse 组合下用的是同一份事实。
- 选中即读取（迭代页）/ 即扫描（文档页）；用户取消什么都不做；
  两条路都不通时在顶栏给一句可读提示，手填路径的老路照常可用。
- 插件自己**不碰文件系统、不拼路径**：拿到的每条路径都是宿主给的绝对路径。
- `Button` 构件补了 `title` 参数（原生悬停说明）。
- 客户端半侧只多了几个 `ctx.get` 读服务的调用：没有新增宿主路由、没有新增写操作、没有新增依赖。

### 一键回当前会话

- 面板右上角加了 **← 返回对话**：调布局服务的 `selectPanel(null)`——它的契约就是「null 显示当前会话」，
  且**不改变当前会话**，只是把中央区域从面板换回会话界面（和再点一次左栏那枚图标是同一件事）。
- 宿主没有布局服务时**不渲染**这个按钮：点了也没用的东西不放。

### 上一步 / 下一步：面板里也有浏览历史

- 标题行最左加了 **← →** 两个按钮，走面板自己的历史栈：切页签、换目录、打开一个技能、
  点开某一轮、改筛选——每一步都记进去，能像浏览器那样后退/前进，悬停显示还剩几步。
- 只有「用户明确做的一步」入栈。列表数据、加载中、悬停卡这些是**结果**不是步骤，不入栈；
  输入框里的连续打字也不入（回车 / 读取 / 扫描才算一步）。
- 为此把三页的**位置状态**（当前页签；迭代页的目录、选中轮次、筛选；文档页的目录；
  技能库打开的技能与来源过滤）提升到面板层统一管，各页只留数据与加载状态。
  附带两个好处：切页签再切回来不再丢位置；后退/前进回到某页时，会按那个位置重新拉一次数据。
- 后退之后再做一个新动作会清掉「下一步」那条线（和浏览器一致）。
- 两个箭头是**手写 SVG chevron**（17px、线宽 2.2、圆角），装在 28×28 的方形按钮里：
  悬停时底色浮起一档、边框转强调色，不可用时半透明置灰。原先那个 12px 的文字 `←` `→`
  又细又小，放大还容易跟中文字形不搭。

### 目录里多了内容：自动检测 + 手动刷新

- 目录行多了一个**档位按钮**：`自动：10 秒` → `30 秒` → `1 分钟` → `关`，点一下换一档，选择落在 localStorage。
  默认 10 秒——产线的记录是「跑完一个脚本追加一行」，等一分钟才看见太钝。迭代页与文档页各有一个，共用同一个档位。
- **自动检测只负责发现，不擅自改屏幕**：探到内容变了，顶栏出现一条提示
  「这个目录有新内容：多了 N 轮」+【现在刷新】【稍后】。你正看着一屏时间轴，它自己跳走比不刷新更烦。
- 【稍后】= 这一版变化先不管，等内容**再变一次**才会重新提醒；【现在刷新】后提示立刻收起，也不会再报同一批。
- 页面在后台（`document.visibilityState === 'hidden'`）时那一拍直接跳过：看不见的时候刷新没意义，还白白读盘。
- 探不到目录（被删、读不动）就当没变化，不打扰。
- **没有新增宿主路由**：检测就是把已有的 `/history`（或 `/docs`）再读一遍、算个指纹做比较——
  迭代页取 `id|审批状态|体积|快照路径`，文档页取 `主干|配对状态|两边 mtime`。
  所以这条也和前面几步一样，改完客户端存盘即生效。
- 「手动刷新」本来就有：迭代页的【读取】、文档页的【扫描】，点同一个目录就是重读一遍。

### 自检

- `node test/smoke.mjs` —— **129 项**（本次新增 57 项）：
  - 目录选择：按钮存在与悬停说明、点击后调服务、原生选中即扫描、取消不发请求、
    原生被拒时自动开应用内浏览器、从主目录开始列、进子目录、面包屑显示完整层级、采纳即扫描、采纳后关闭、
    迭代页同样两条路、两条路都不通时的提示、没有该服务时降级为手填；
  - 返回对话：按钮存在、悬停说明写清了它做什么、点击后 `selectPanel(null)`、
    没有布局服务时不渲染这个按钮且函数如实返回 false；
  - 上一步/下一步：初始两个都灰、切页签与打开技能各记一步、悬停报出剩余步数、
    连退两步（技能全文收起 → 回到迭代页）、退到底变灰、连进两步回到原状态、走到头变灰、
    退回去后做新动作清掉「下一步」；外观那几条也在里面（svg 图标而非文字箭头、图标 ≥16px、
    按钮 28×28、灰掉时半透明、悬停底色浮起一档、移开落回去）；
  - 自动检测：默认档位与已排定时器、探到新记录只提示不改屏幕、【稍后】后同一批不重复提醒、
    再变一次又提醒（按屏幕上的内容算，报「多了 2 轮」）、【现在刷新】后提示消失且新记录进时间轴、
    刷新完不再报同一批、档位循环到关后不再排定时器。
  - 定时器在测试里被换成收集器（真的 `setInterval` 会让 Node 挂着不退出），需要时用手动 tick 触发。
- `node test/host-routes.mjs` —— 49 项（本次未改宿主半侧，回归通过）。

## [0.1.0] — 2026-09-18

首个公开版本。

### 三个页签

- **迭代（默认）** —— 把「AI 改 Markdown → 生成 Word/PDF/Excel → 人审这一版」变成一条时间轴：
  - 按产线分组、产线内按天分小节，一轮一行：时间 / 产物类型 / 这一版干了什么 / 改自哪几个 md / 审批状态；
  - 鼠标悬停浮出摘要卡：摘要、产出时间与体积、生成命令、改动来源、快照路径、历史意见；
  - 点开一轮可【通过】【要改】【重置为待审】，写进产线目录下的 `.versions/decisions.jsonl`（追加式，不改历史）；
  - 顶部按状态 / 产线 / 类型筛选；「载入演示」一键加载自带假数据。
- **技能库** —— 读核心 `ctx.skills` 注册表（模型实际加载的同一份目录），可搜索、按来源过滤、读 `SKILL.md` 全文。
- **文档** —— 把 md/tex 源与 docx/pdf 产物按主干配对，按「该先处理」排序（产物过期 / 无源产物 / 未生成 / 已同步）。

### 记录格式（时间轴的数据来源）

时间轴不是猜出来的，是产出时记下来的。每个产线目录：

```
产线/
├── *.md                          ← 源
├── 论文全文.docx                  ← 工作副本，名字永远不变
└── .versions/
    ├── produced.jsonl            ← 每次产出追加一行（摘要 / 来源 / 工具 / 快照）
    ├── decisions.jsonl           ← 每次审批追加一行
    └── 20260911T2210-论文全文.docx ← 快照
```

### 宿主半侧

- 7 条 JSON 路由（`/api/dsh-workbench/*`）：`/skills`、`/skill`、`/docs`、`/workspaces`、`/history`、`/demo` 为只读；
  `/decide`（POST）是**唯一**的写操作，只往 `<产线>/.versions/decisions.jsonl` 追加一行，带路径越界与状态校验。
- 零运行时依赖：宿主半侧只用 `node:*`，客户端半侧只用外壳提供的 react 与浏览器 `fetch`。

### 工作台出现在哪里

| 落点 | 出现在哪 | 条件 |
|---|---|---|
| 官方左栏面板座位 | 系统左栏的面板图标行（`sidebar.panellist` + `main`） | 总是注册 |
| 侧栏页签 | 若装了 `dsh-better-sidebar`，工作台也会作为它的一个页签出现 | 装了才注册 |

`dsh-better-sidebar` 是可选依赖（`ctx.get('betterSidebar')`），读不到就只走官方座位，不报错、不挂起。

### 演示数据

`examples/thesis-workbench/`：3 条产线（毕业论文 14 轮 / Aspen 流程模拟 8 轮 / 经济分析 5 轮），
内容全是假的，结构是真的。`node examples/make-demo.mjs` 可重新生成。

### 自检（都不需要浏览器）

- `node test/smoke.mjs` —— 69 项：桩 `__ModuleLoader__` + 迷你 React 跑真实组件树，
  覆盖「载入演示 → 渲染产线/轮次/按天 → 悬停出摘要卡 → 点击审批 → 写记录后状态变已通过」整条回路，
  以及双落点注册与「没装 better-sidebar 时的降级」。
- `node test/host-routes.mjs` —— 46 项：假 req/res 逐条调路由 + 真实文件系统扫描 + 在临时目录真写一次审批。

### 已知限制

- 界面观感只过了组件树自检，**未做人工视觉验证**。
- 历史里已经同名覆盖掉的版本找不回来（快照从设了记录器之后才开始存）。
- 「产物过期」按文件修改时间判定，不看内容。
- 目录扫描不递归，一次最多 1000 轮。
- 审批只写记录文件，不会真的改动或回滚你的 Word。
