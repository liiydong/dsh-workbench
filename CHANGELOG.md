# 变更日志

本文件记录 dsh-workbench 每个版本做了什么。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.4.0] — 2026-09-19

### 和上一版比：这一版到底改了什么

README 开头就写着「你也不知道这版跟前一版差在哪」——时间轴把版本存下来了，
但** diff 一直没有**。这一步补上它：点开任意一轮，底部多一枚【和上一版比】。

- 默认跟**上一轮**比；顶上下拉换成同一条产线里更早的任意一轮；
- 读的是两边的**快照**（`.versions/` 里那一份）。这一轮没留快照时退回读工作副本，
  标题行把两个文件名都摆出来，不遮着；
- **段落级 diff**：新增标绿、删掉标红。一删一添配成一对时，段内**再按词高亮**——
  `0.62 → 0.58` 自己跳出来，不用整段读完再自己找；
- 连续没动的地方折成一行「…… 中间 N 段没有变化 ……」；
- 两版一字不差时明说「**两版正文完全一致**」：脚本跑了但没改，和「改了但看不出来」是两件事；
- 统计一行：`新增 N 段 · 删掉 M 段 · 相同 K 段`。

### 新增 `GET /text`：把一版产物的正文读出来

差异要在浏览器里算，就得有人把两段文本交出来。新路由只做这件事：

- **docx 自己解**：解析 zip 的中央目录（零依赖，解压用 `node:zlib`），
  取 `word/document.xml`，折成段落 / 表格行 / 标题块；
- md / tex / qmd / txt / csv / html 当**纯文本**读（md 的标题与列表照原样分级）；
- pdf / xlsx / pptx **不在白名单里**：读不出就不读，面板上如实说，不编内容；
- 与 `/reveal` 同一套围栏：产线名只能是根目录下的直接子目录名，`file` 可以带子目录
  （快照就在 `.versions/` 下）但不接受绝对路径、盘符与 `..`，目标必须落在项目根目录内；
- 上限说在前头：单个文件 24 MiB、一段 1200 字、一次 4000 段，超了在 `truncated` 里说明。

这个接口**只读**、不启动任何程序，所以会碰外界的仍然只有原来那三个（decide / adopt / reveal）。

### 演示数据里的 Word 变成**真的** docx

原来演示的「Word」是改名成 .docx 的文本文件，点【和上一版比】只会得到一句「读不出」，
演示就白放了。现在 `make-demo.mjs` 会真打一个 zip + `word/document.xml`（deflate 压缩），
内容还是假的、结构是真的：每一轮都带几段固定内容，于是相邻两版之间**既有改动、又有没动的部分**，
折叠提示与词级高亮都看得见。

### 只比产物，不比 md 源（一处故意的取舍）

一开始做了个「比 md 源」的模式，做出来发现是个坑：磁盘上 md 只有一份、随时被改写，
「这一轮记的源」和「那一轮记的源」读到的往往是**同一个文件**，比出来永远是「完全相同」——
那不是没改，是根本没有旧版可比。所以这一版把它去掉了，只老老实实比产物快照。

### 本轮自检（两项合起来）

- `node test/smoke.mjs` —— **230 项**（本轮从 184 加到 230）：
  - 和上一版比（30 项）：按钮出现与置灰、点开读两版、时间轴让位、增删统计、
    配对的一删一添**两行都画**（少画任何一边就不知道该改成什么样）、段内词级高亮、
    未改动段折叠、两版一致时明说、换基线重读、读不出时给原因、收起后不再读；
  - 差异算法边界（14 项）：空序列、中间插入、两头都改、段尾空格不算差异、
    超大序列退化成全删全增、中文按字切、英文按词切、颜色加透明度；
  - 桩的 `/history` 现在也按时间新→旧排（跟真宿主一致），否则「上一版」指不定是谁。
- `node test/host-routes.mjs` —— **139 项**（本轮从 101 加到 139）：
  - zip 解析：deflate 与 stored 都能解、取不到的条目返回 null、不是 zip 时不当崩溃；
  - docx 折块：跨多个 run 拼成一段、`Heading 1` / `标题 2` / 数字样式 / 大纲级别都认成标题、
    列表项标 li、表格行用竖线连起来、XML 实体解码、**域代码（页码那种）不当正文**；
  - `/text` 路由：读 docx 与 md、快照在 `.versions/` 子目录里也读得到、`line=.`（单产线项目）、
    404 / 400 的各条边界（穿越、绝对路径、坏产线名、白名单外类型）、只接受 GET、
    不是 zip 的 docx 给 `ok:false` 而不是 500、超长文档截断且如实标记。

## [0.3.0] — 2026-09-19

### 一边看对话，一边看时间轴：工作台能停到右栏了

之前工作台只能占**主区域**——点开它，对话就被挤走；而「改一章 → 扫一眼这一版」这个动作本来
就得两边对照着看。现在面板右上角多了一个 **搬到右栏**：请宿主把工作台开在右侧栏
（`ctx.sidebarRight.openTab('dsh-workbench')`），对话留在中间不动。

它同时是官方右栏的一种**页签类型**（`sidebarRight.tabs.register` + keyed 座位
`sidebar.right.pane.tab`），所以右栏「+」里的引导页也能找到它，标题就是「工作台」。
已经是右栏那一份时，这枚按钮自己收起来（点它没意义）；宿主没有右栏能力
（旧版 DSH）时按钮和注册都不出现，左栏那套照常能用。

### 左栏图标上的「待我审」角标

不用点开就知道有没有活干：左栏那枚工作台图标旁边画一个数字，是当前项目里**还没审的轮数**；
数字为 0 时整个角标不渲染（不画一个 0 占地方），超过 99 收成 `99+`。

- 数字从哪来：面板把屏幕上那份数据的 `totals.pending` 报给一个模块级小 store（十几行订阅），
  图标订阅它。**打开插件时不额外请求**——面板自己读过一次记录，顺手就把数报上去了。
- 之后按**自动档位**（和迭代页那个「自动：10 秒 / 30 秒 / 1 分钟 / 关」同一个档位）轮询刷新；
  页面在后台时那一拍跳过。
- 装了 `dsh-better-sidebar` 的话，它自己的页签角标能力（`TabDescriptor.badge`）也报同一个数——
  能力要先在 `service.features` 里声明才用（`features.includes('badge')`），没声明就不带这个字段。

### 把「漏掉的版本」捡回来：未入记录 → 收编成一版

这个插件一直有个说不出口的短板：时间轴读的是 `.versions/produced.jsonl`，而记录得由产出方写；
真实项目里往往一个脚本跑完就覆盖了 Word，**一行记录都没有**，于是迭代页永远是空的。
这一步补的就是它，分两半：

- **找**（宿主 `POST /scan`）：列出「文件躺在产线目录里、记录里却没有」的产物。
  判据只有一条：记录里有同名产物、且记录时间不早于文件时间（留 3 秒时钟差，吸收先复制快照后写记录的次序）。
  排除 `.versions/`、`~$` 临时文件，以及与某份源同名的导出副本（`报告.docx.pdf` 那种）。
- **收编**（宿主 `POST /adopt`）：你补一句话、点【记一版】，才真的收编 ——
  **复制一份快照**到 `.versions/` + 往 `produced.jsonl` **追加一行**（带 `adopted: true`）。
  快照同名时**只新建不覆盖**：同一分钟内重复点会给 409 和一句人话，而不是悄悄覆盖掉刚才那一版。

面板上：迭代页顶栏多了 **未入记录** 按钮（默认不展开，**点开才扫**——扫一次要读目录、还可能翻会话日志）；
展开后按产线列出每个漏记的产物，每行一个「这一版改了什么」输入框 + 【记一版】按钮。
收编成功后那一条从清单里消失、时间轴立刻多出一轮。

### 「这条命令生成的」是从 DSH 会话日志里反推的，而且说得保守

`/scan` 会调 DSH 的会话查询服务（`ctx.sessionQuery`：`listSessions` + `readSession`），
只看 **cwd 等于该项目目录** 的会话，把 `tool/call` 事件折成「谁在什么时候碰了这个文件」。归因规则刻意收窄：

- 用「写文件」工具（`write` / `edit`）写的 → 认；
- 命令行则要求路径附近有**写**的信号：`--out` / `--output` / `-o` / 重定向，或产出类动词 + 目标文件名；
- 命令里出现删除 / 移动 / 查看类动词（`Remove-Item`、`move`、`Get-Content`…）→ 不认；
- 反例是真实踩到的：`soffice --convert-to pdf 论文.docx --outdir …` 里那个 docx 是**读入**的一方，
  把它当生成命令，时间轴上就多一条假历史。**宁可少给**：找不到就显示「没找到生成命令（可能是别的工具改的）」。
- 上限：最多读最近 40 个会话 / 20000 条事件；会话服务缺席时整段降级，不影响收编。

### 安全边界（`/adopt` 是第三个会写文件的接口）

- 产线名只能是根目录的**直接子目录名**、文件名只能是产线目录里的**文件名**（拒绝 `..` 与分隔符）；
- 目标必须落在项目根目录内，且**真的存在**、是**允许的产物类型**（docx/doc/pdf/xlsx/pptx/html/csv/png/jpg）；
- 只往 `<产线>/.versions/` 里写；**不动你的 Word 本身**，也不碰任何已有文件的路径。

### 看完一版，下一步能直接打开它

点开某一轮后，底部那条上多三个按钮：

- **打开这一版** —— 用系统默认程序打开那一版的**快照**（`.versions/2026…-论文全文.docx`）；
- **打开成品** —— 打开产线目录里那个名字永远不变、随时被覆盖的最新成品；
- **在文件夹里** —— 打开文件管理器并**选中**它（Windows `explorer /select,`、macOS `open -R`、Linux `xdg-open`）。

新增宿主路由 `POST /reveal`，是本插件**第二个会碰外界的操作**（第一个是记审批）。它的边界就是全部规则：

- 只认**白名单里的文档类型**（docx / doc / pdf / xlsx / xls / pptx / md / txt / csv / html / png / jpg）；
  放行任意路径等于给网页一个「执行任意程序」的接口（`.exe` / `.bat` / `.lnk`）——这条是安全边界，不是偏好；
- 只认**项目根目录里的文件**：拒绝 `..`、分隔符、以及不像产线目录名的 `line`；
- 文件必须**真的存在**、且确实是文件（快照被挪走后给的是 404 加一句人话，不是 500）。

三个平台各一套启动方式，且都**不经过 shell**（参数以数组交给 `spawn`），路径里的空格或 `&` 不会被当成命令解析。
自检里把「真的启动程序」那一步换成了收集器——测试绝不能弹出资源管理器窗口。

### 本轮自检（三项合起来）

- `node test/smoke.mjs` —— **184 项**（本轮从 137 加到 184）：
  - 打开某一版（8 项）：三个按钮存在、快照路径、成品文件名、reveal 模式、产线名、不动审批状态；
  - 未入记录（20 项）：默认不扫、展开才扫、列出漏记产物、显示生成方式与改过的源、
    没找到生成命令时如实说、备注进请求、收编成功提示快照路径、收编后重扫且该条从清单消失、
    时间轴跟着刷新、失败时显示原因且不清空清单、能收起；
  - 右栏停靠（14 项）：右栏类型注册（kind 与座位同 id、声明为扩展、标题、引导卡片与它的图标）、
    右栏正文渲染的就是工作台、在右栏时「搬到右栏」收起、主区域有这枚按钮且点了会 `openTab`、
    没有右栏服务时不注册也不显示按钮、两个函数如实回报不可用；
  - 角标（11 项）：面板同屏渲染时报的数与面板一致、悬停说明带目录名、超过 99 收成 `99+`、
    归零后角标不渲染、better-sidebar 页签角标跟着同一个数走。
  - 桩的 `slots.inject` 现在也接受**生成器**（右栏座位就是这么注册的）。
- `node test/host-routes.mjs` —— **101 项**（本轮从 63 加到 101）：`/scan` 只翻本项目 cwd 的会话、
  归因反例（soffice 读入方、删除命令）、带出改过的源；`/adopt` 真写快照 + 追加记录、
  收编后不再出现在清单里、同分钟 409、白名单外拒绝、路径穿越拒绝、非法产线名、文件不存在 404、
  只收 POST、缺参数 400、目录不存在给可读原因；另有 11 项归因判据与「已记录」判定的单元样例。
- `node examples/make-demo.mjs` 顺带修了个真问题：演示数据现在会把产物与快照的 mtime
  拨回记录里那一刻，否则整个演示项目会被【未入记录】报成「全部漏记」。

⚠️ 前端三项（打开某一版 / 右栏停靠 / 角标）改完存盘即生效（客户端半边有热重载）；
**宿主半侧**这一轮新增了 `/reveal`、`/scan`、`/adopt` 三条路由，要**重启一次 `dsh web`** 才生效。

## [0.2.0] — 2026-09-19

### 目录不用手打了：迭代页与文档页各加一个「浏览…」

- 点「浏览…」开的是**宿主的目录选择**，两条路自动挑：
  1. 宿主挂的是**操作系统原生选择器**（本地 127.0.0.1 部署）→ 弹系统的选文件夹对话框；
  2. 宿主挂的是 **browse 后端**（远程 / SSH 启动 / 其它部署——**本机实测就是这种**）→
     原生 `pick()` 会被宿主明确拒绝（原文 `directoryPicker.pick needs the native capability; the composed picker serves "browse"`），
     这时自动改用**插件自绘的应用内目录浏览器**：面包屑 + 子目录列表（隐藏项淡化）+ 手输路径 + 上一层 / 主目录。
     它走同一个后端的 `listDirectory`，和 DSH 官方「添加工作区」在 browse 组合下用的是同一份事实。
- 选中即读取（迭代页）/ 即扫描（文档页）；用户取消什么都不做；
  两条路都不通时在顶栏给一句可读提示，手填路径的老路照常可用。
- 插件自己**不碰文件系统、不拼路径**：拿到的每条路径都是宿主给的绝对路径。
- `Button` 构件补了 `title` 参数（原生悬停说明）。
- 客户端半侧只多了几个 `ctx.get` 读服务的调用：没有新增宿主路由、没有新增写操作、没有新增依赖。

### 一键回当前会话

- 面板右上角加了 **← 返回对话**：调布局服务的 `selectPanel(null)`——它的契约就是「null 显示当前会话」，
  且**不改变当前会话**，只是把中央区域从面板换回会话界面（和再点一次左栏那枚图标是同一件事）。
- 宿主没有布局服务时**不渲染**这个按钮：点了也没用的东西不放。

### 上一步 / 下一步：面板里也有浏览历史

- 标题行最左加了 **← →** 两个按钮，走面板自己的历史栈：切页签、换目录、打开一个技能、
  点开某一轮、改筛选——每一步都记进去，能像浏览器那样后退/前进，悬停显示还剩几步。
- 只有「用户明确做的一步」入栈。列表数据、加载中、悬停卡这些是**结果**不是步骤，不入栈；
  输入框里的连续打字也不入（回车 / 读取 / 扫描才算一步）。
- 为此把三页的**位置状态**（当前页签；迭代页的目录、选中轮次、筛选；文档页的目录；
  技能库打开的技能与来源过滤）提升到面板层统一管，各页只留数据与加载状态。
  附带两个好处：切页签再切回来不再丢位置；后退/前进回到某页时，会按那个位置重新拉一次数据。
- 后退之后再做一个新动作会清掉「下一步」那条线（和浏览器一致）。
- 两个箭头是**手写 SVG chevron**（17px、线宽 2.2、圆角），装在 28×28 的方形按钮里：
  悬停时底色浮起一档、边框转强调色，不可用时半透明置灰。原先那个 12px 的文字 `←` `→`
  又细又小，放大还容易跟中文字形不搭。

### 目录里多了内容：自动检测 + 手动刷新

- 目录行多了一个**档位按钮**：`自动：10 秒` → `30 秒` → `1 分钟` → `关`，点一下换一档，选择落在 localStorage。
  默认 10 秒——产线的记录是「跑完一个脚本追加一行」，等一分钟才看见太钝。迭代页与文档页各有一个，共用同一个档位。
- **自动检测只负责发现，不擅自改屏幕**：探到内容变了，顶栏出现一条提示
  「这个目录有新内容：多了 N 轮」+【现在刷新】【稍后】。你正看着一屏时间轴，它自己跳走比不刷新更烦。
- 【稍后】= 这一版变化先不管，等内容**再变一次**才会重新提醒；【现在刷新】后提示立刻收起，也不会再报同一批。
- 页面在后台（`document.visibilityState === 'hidden'`）时那一拍直接跳过：看不见的时候刷新没意义，还白白读盘。
- 探不到目录（被删、读不动）就当没变化，不打扰。
- **没有新增宿主路由**：检测就是把已有的 `/history`（或 `/docs`）再读一遍、算个指纹做比较——
  迭代页取 `id|审批状态|体积|快照路径`，文档页取 `主干|配对状态|两边 mtime`。
  所以这条也和前面几步一样，改完客户端存盘即生效。
- 「手动刷新」本来就有：迭代页的【读取】、文档页的【扫描】，点同一个目录就是重读一遍。

### 自检

- `node test/smoke.mjs` —— **129 项**（本次新增 57 项）：
  - 目录选择：按钮存在与悬停说明、点击后调服务、原生选中即扫描、取消不发请求、
    原生被拒时自动开应用内浏览器、从主目录开始列、进子目录、面包屑显示完整层级、采纳即扫描、采纳后关闭、
    迭代页同样两条路、两条路都不通时的提示、没有该服务时降级为手填；
  - 返回对话：按钮存在、悬停说明写清了它做什么、点击后 `selectPanel(null)`、
    没有布局服务时不渲染这个按钮且函数如实返回 false；
  - 上一步/下一步：初始两个都灰、切页签与打开技能各记一步、悬停报出剩余步数、
    连退两步（技能全文收起 → 回到迭代页）、退到底变灰、连进两步回到原状态、走到头变灰、
    退回去后做新动作清掉「下一步」；外观那几条也在里面（svg 图标而非文字箭头、图标 ≥16px、
    按钮 28×28、灰掉时半透明、悬停底色浮起一档、移开落回去）；
  - 自动检测：默认档位与已排定时器、探到新记录只提示不改屏幕、【稍后】后同一批不重复提醒、
    再变一次又提醒（按屏幕上的内容算，报「多了 2 轮」）、【现在刷新】后提示消失且新记录进时间轴、
    刷新完不再报同一批、档位循环到关后不再排定时器。
  - 定时器在测试里被换成收集器（真的 `setInterval` 会让 Node 挂着不退出），需要时用手动 tick 触发。
- `node test/host-routes.mjs` —— 49 项（本次未改宿主半侧，回归通过）。

## [0.1.0] — 2026-09-18

首个公开版本。

### 三个页签

- **迭代（默认）** —— 把「AI 改 Markdown → 生成 Word/PDF/Excel → 人审这一版」变成一条时间轴：
  - 按产线分组、产线内按天分小节，一轮一行：时间 / 产物类型 / 这一版干了什么 / 改自哪几个 md / 审批状态；
  - 鼠标悬停浮出摘要卡：摘要、产出时间与体积、生成命令、改动来源、快照路径、历史意见；
  - 点开一轮可【通过】【要改】【重置为待审】，写进产线目录下的 `.versions/decisions.jsonl`（追加式，不改历史）；
  - 顶部按状态 / 产线 / 类型筛选；「载入演示」一键加载自带假数据。
- **技能库** —— 读核心 `ctx.skills` 注册表（模型实际加载的同一份目录），可搜索、按来源过滤、读 `SKILL.md` 全文。
- **文档** —— 把 md/tex 源与 docx/pdf 产物按主干配对，按「该先处理」排序（产物过期 / 无源产物 / 未生成 / 已同步）。

### 记录格式（时间轴的数据来源）

时间轴不是猜出来的，是产出时记下来的。每个产线目录：

```
产线/
├── *.md                          ← 源
├── 论文全文.docx                  ← 工作副本，名字永远不变
└── .versions/
    ├── produced.jsonl            ← 每次产出追加一行（摘要 / 来源 / 工具 / 快照）
    ├── decisions.jsonl           ← 每次审批追加一行
    └── 20260911T2210-论文全文.docx ← 快照
```

### 宿主半侧

- 7 条 JSON 路由（`/api/dsh-workbench/*`）：`/skills`、`/skill`、`/docs`、`/workspaces`、`/history`、`/demo` 为只读；
  `/decide`（POST）是**唯一**的写操作，只往 `<产线>/.versions/decisions.jsonl` 追加一行，带路径越界与状态校验。
- 零运行时依赖：宿主半侧只用 `node:*`，客户端半侧只用外壳提供的 react 与浏览器 `fetch`。

### 工作台出现在哪里

| 落点 | 出现在哪 | 条件 |
|---|---|---|
| 官方左栏面板座位 | 系统左栏的面板图标行（`sidebar.panellist` + `main`） | 总是注册 |
| 侧栏页签 | 若装了 `dsh-better-sidebar`，工作台也会作为它的一个页签出现 | 装了才注册 |

`dsh-better-sidebar` 是可选依赖（`ctx.get('betterSidebar')`），读不到就只走官方座位，不报错、不挂起。

### 演示数据

`examples/thesis-workbench/`：3 条产线（毕业论文 14 轮 / Aspen 流程模拟 8 轮 / 经济分析 5 轮），
内容全是假的，结构是真的。`node examples/make-demo.mjs` 可重新生成。

### 自检（都不需要浏览器）

- `node test/smoke.mjs` —— 69 项：桩 `__ModuleLoader__` + 迷你 React 跑真实组件树，
  覆盖「载入演示 → 渲染产线/轮次/按天 → 悬停出摘要卡 → 点击审批 → 写记录后状态变已通过」整条回路，
  以及双落点注册与「没装 better-sidebar 时的降级」。
- `node test/host-routes.mjs` —— 46 项：假 req/res 逐条调路由 + 真实文件系统扫描 + 在临时目录真写一次审批。

### 已知限制

- 界面观感只过了组件树自检，**未做人工视觉验证**。
- 历史里已经同名覆盖掉的版本找不回来（快照从设了记录器之后才开始存）。
- 「产物过期」按文件修改时间判定，不看内容。
- 目录扫描不递归，一次最多 1000 轮。
- 审批只写记录文件，不会真的改动或回滚你的 Word。
